# Pre-trained Models

Store any pretrained `.pt` file in this folder for fine-tuning.

Examples:

- `pre_trained_model/yolo11n.pt`
- `pre_trained_model/custom_detector.pt`

Set `finetune_model` in `configs/train.yaml` (or other train config) to choose which `.pt` file to use.
